name: Chequeo de licencias en proyectos Java con Gradle

on:
  push:
    branches:
      - master

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
      
      - name: License check
        run: ./gradlew licenseFormatCheck
      
      - name: License report
        run: ./gradlew licenseFormatReport


